# Portfolio (Next.js + Tailwind + TypeScript)

Minimal starter for a personal portfolio using Next.js (App Router), Tailwind CSS, TypeScript, lucide-react and framer-motion.

To run locally:

```bash
npm install
npm run dev
```
